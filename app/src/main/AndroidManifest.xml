<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.youth_and_elderly_liveing_together_new">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Youth_and_Elderly_liveing_Together_new"
        tools:targetApi="31">

        <activity
            android:name=".CallDoctor"
            android:exported="false" />

        <activity
            android:name=".AskMedical"
            android:exported="false" />

        <activity
            android:name=".askmedical"
            android:exported="false" />

        <activity
            android:name="com.example.customlistview.DetailedActivity"
            android:exported="false" /> <!-- 指定啟動活動 -->

        <activity
            android:name=".DailyActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
